# Client

The `RecapClient` is the main entry point for all interactions with a RECAP
database. Use `RecapClient.from_sqlite()` to create or connect to a local
SQLite database.

::: recap.client.RecapClient
